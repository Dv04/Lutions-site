/** @type {import('next').NextConfig} */
const nextConfig = {
    // You can add future configurations here, such as redirects, rewrites, or image domains.
    images: {
        // If you use external images, add their hostnames here
        // remotePatterns: [
        //   {
        //     protocol: 'https',
        //     hostname: 'example.com',
        //   },
        // ],
    },
};

export default nextConfig;
